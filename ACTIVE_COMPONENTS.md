# Active Components

This repo currently uses the v2 pipeline:

- GUI: `src/main_gui.cpp` with `src/ui.cpp`, `src/model.cpp`, `src/db.cpp`
- Importer: `src/importer/`
- Primary database: `data/evony_v2.db`

Archived (not used in current workflow):

- Legacy GUI code: `archive/legacy_src/gui/`
- Legacy entrypoint: `archive/legacy_src/main.cpp`
- Legacy parser/importer/compute sources: `archive/legacy_src/`
- Legacy headers from old architecture: `archive/legacy_include/`
- Legacy database file: `archive/legacy_db/evony.db`
